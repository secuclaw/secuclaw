apiVersion: v1
kind: Service
metadata:
  name: {{ include "secuclaw.fullname" . }}-web
  labels:
    {{- include "secuclaw.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secuclaw.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
