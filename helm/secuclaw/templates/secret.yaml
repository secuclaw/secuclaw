apiVersion: v1
kind: Secret
metadata:
  name: {{ include "secuclaw.fullname" . }}
  labels:
    {{- include "secuclaw.labels" . | nindent 4 }}
type: Opaque
stringData:
  LLM_API_KEY: {{ .Values.config.llm.apiKey | default "" }}
