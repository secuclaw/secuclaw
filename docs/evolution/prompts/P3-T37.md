# 任务卡片: P3-T37 Tailscale隧道集成

## 任务元数据

```yaml
任务ID: P3-T37
任务名称: Tailscale Tunnel Integration
Phase: Phase 3 - 边缘部署 + 轻量化
类型: 代码实现
复杂度: ⭐⭐⭐
预估时间: 2天
建议LLM: Claude 3.5 Sonnet / GPT-4
```

---

## 任务描述

集成Tailscale隧道，支持安全的私有网络访问。支持Serve和Funnel两种模式。

---

## 输入资源

### 参考项目

#### ZeroClaw - Tailscale隧道
```
zeroclaw-0.1.7/src/tunnel/
├── tailscale.rs        # Tailscale实现
└── mod.rs              # 隧道模块
```

### 目标位置
```
secuclaw/packages/core/src/tunnel/tailscale/
├── client.ts          # 客户端
├── serve.ts           # Serve模式
├── funnel.ts          # Funnel模式
├── auth.ts            # 认证
├── types.ts           # 类型定义
└── *.test.ts          # 测试
```

### 现有代码参考
```
secuclaw/packages/core/src/security/       # 安全模块
secuclaw/packages/edge/src/runtime.ts      # 边缘运行时
```

---

## 输出要求

### 必须产出的文件

1. **client.ts** - 客户端
   ```typescript
   export class TailscaleClient {
     // 连接
     async connect(): Promise<void>;
     
     // 断开
     async disconnect(): Promise<void>;
     
     // 获取状态
     getStatus(): TailscaleStatus;
   }
   ```

2. **serve.ts** - Serve模式
   - `TailscaleServe` 类
   - `serve()` - 暴露服务
   - `stop()` - 停止服务
   - `list()` - 列出服务

3. **funnel.ts** - Funnel模式
   - `TailscaleFunnel` 类
   - `enable()` - 启用Funnel
   - `disable()` - 禁用Funnel
   - `getStatus()` - 获取状态

4. **auth.ts** - 认证
   - `TailscaleAuth` 类
   - `login()` - 登录
   - `logout()` - 登出
   - `checkAuth()` - 检查认证

---

## 验收标准

### 功能验收
- [ ] 支持Serve模式
- [ ] 支持Funnel模式
- [ ] 支持身份验证
- [ ] 自动重连

### 安全验收
- [ ] 认证验证
- [ ] 访问控制
- [ ] 加密传输

---

## 技术约束

### 必须遵守
- 使用Tailscale API
- 支持认证
- 支持重连
- 错误处理

### 禁止
- 不要硬编码密钥
- 不要明文传输
- 不要忽略错误

---

## Prompt模板

```
你是一个专业的网络工程师，负责为SecuClaw集成Tailscale隧道。

## 背景
SecuClaw需要支持Tailscale安全隧道访问。你需要实现Serve和Funnel两种模式。

## 参考代码

### ZeroClaw Tailscale隧道
[读取并参考: zeroclaw-0.1.7/src/tunnel/tailscale.rs]

### ZeroClaw隧道模块
[读取并参考: zeroclaw-0.1.7/src/tunnel/mod.rs]

### SecuClaw安全模块
[读取并参考: secuclaw/packages/core/src/security/]

### SecuClaw边缘运行时
[读取并参考: secuclaw/packages/edge/src/runtime.ts]

## 任务要求

1. 在 `secuclaw/packages/core/src/tunnel/tailscale/` 目录下创建文件：
   - client.ts
   - serve.ts
   - funnel.ts
   - auth.ts
   - types.ts
   - 测试文件

2. 实现Tailscale客户端：
   - 连接管理
   - 状态查询
   - 错误处理

3. 实现Serve模式：
   - 服务暴露
   - 端口映射
   - 访问控制

4. 实现Funnel模式：
   - 公网访问
   - 域名绑定
   - 状态监控

5. 实现认证：
   - 登录登出
   - 认证检查
   - 密钥管理

## 验收标准
- TypeScript无错误
- 支持Serve和Funnel
- 支持认证

请开始实现，提供完整的代码。
```

---

## 示例代码片段

### client.ts 骨架

```typescript
import { exec } from 'child_process';
import { promisify } from 'util';

const execAsync = promisify(exec);

export class TailscaleClient {
  private connected: boolean = false;

  /**
   * 连接
   */
  async connect(): Promise<void> {
    try {
      await execAsync('tailscale up');
      this.connected = true;
    } catch (error) {
      throw new Error(`Failed to connect: ${error}`);
    }
  }

  /**
   * 断开
   */
  async disconnect(): Promise<void> {
    try {
      await execAsync('tailscale down');
      this.connected = false;
    } catch (error) {
      throw new Error(`Failed to disconnect: ${error}`);
    }
  }

  /**
   * 获取状态
   */
  async getStatus(): Promise<TailscaleStatus> {
    try {
      const { stdout } = await execAsync('tailscale status --json');
      return JSON.parse(stdout);
    } catch (error) {
      throw new Error(`Failed to get status: ${error}`);
    }
  }
}
```

---

## Tailscale模式

| 模式 | 用途 | 访问范围 |
|------|------|----------|
| Serve | 本地服务暴露 | Tailscale网络内 |
| Funnel | 公网访问 | 互联网 |

---

## 注意事项

1. **认证**: 确保正确认证
2. **重连**: 支持自动重连
3. **错误处理**: 优雅处理错误
4. **日志记录**: 记录连接状态

---

## 相关链接

- [Tailscale Documentation](https://tailscale.com/kb/)
- [Tailscale Serve](https://tailscale.com/kb/1242/tailscale-serve/)
- [Tailscale Funnel](https://tailscale.com/kb/1223/tailscale-funnel/)
