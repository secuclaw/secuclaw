# 任务卡片: P3-T40 自定义隧道框架

## 任务元数据

```yaml
任务ID: P3-T40
任务名称: Custom Tunnel Framework
Phase: Phase 3 - 边缘部署 + 轻量化
类型: 架构设计
复杂度: ⭐⭐⭐
预估时间: 2天
建议LLM: Claude Opus
```

---

## 任务描述

设计可扩展的隧道框架，支持SSH隧道、WebSocket隧道等自定义实现。使用工厂模式统一管理隧道类型。

---

## 输入资源

### 参考项目

#### ZeroClaw - 自定义隧道
```
zeroclaw-0.1.7/src/tunnel/
├── custom.rs           # 自定义隧道
├── none.rs             # 无隧道
└── mod.rs              # 隧道模块
```

### 目标位置
```
secuclaw/packages/core/src/tunnel/custom/
├── base.ts            # 基类
├── ssh.ts             # SSH隧道
├── ws.ts              # WebSocket隧道
├── factory.ts         # 工厂模式
├── registry.ts        # 注册表
├── types.ts           # 类型定义
└── *.test.ts          # 测试
```

### 现有代码参考
```
secuclaw/packages/core/src/tunnel/tailscale/  # Tailscale参考
secuclaw/packages/core/src/tunnel/ngrok/      # ngrok参考
```

---

## 输出要求

### 必须产出的文件

1. **base.ts** - 基类
   ```typescript
   export abstract class BaseTunnel {
     // 连接
     abstract connect(): Promise<void>;
     
     // 断开
     abstract disconnect(): Promise<void>;
     
     // 检查连接
     abstract isConnected(): boolean;
     
     // 获取URL
     abstract getUrl(): string;
   }
   ```

2. **ssh.ts** - SSH隧道
   - `SSHTunnel` 类
   - 继承BaseTunnel
   - 实现SSH端口转发

3. **ws.ts** - WebSocket隧道
   - `WebSocketTunnel` 类
   - 继承BaseTunnel
   - 实现WebSocket代理

4. **factory.ts** - 工厂模式
   - `TunnelFactory` 类
   - `create()` - 创建隧道
   - `register()` - 注册类型

5. **registry.ts** - 注册表
   - `TunnelRegistry` 类
   - `register()` - 注册隧道
   - `get()` - 获取隧道
   - `list()` - 列出隧道

---

## 验收标准

### 功能验收
- [ ] 支持扩展
- [ ] 支持SSH隧道
- [ ] 支持WS隧道
- [ ] 统一接口

### 架构验收
- [ ] 工厂模式
- [ ] 注册表模式
- [ ] 可扩展性

---

## 技术约束

### 必须遵守
- 使用抽象类
- 统一接口
- 工厂模式
- 可扩展

### 禁止
- 不要硬编码类型
- 不要耦合实现
- 不要限制扩展

---

## Prompt模板

```
你是一个资深架构师，负责为SecuClaw设计自定义隧道框架。

## 背景
SecuClaw需要支持多种隧道类型，需要可扩展的框架。你需要设计基类、工厂和注册表。

## 参考代码

### ZeroClaw自定义隧道
[读取并参考: zeroclaw-0.1.7/src/tunnel/custom.rs]

### ZeroClaw无隧道
[读取并参考: zeroclaw-0.1.7/src/tunnel/none.rs]

### ZeroClaw隧道模块
[读取并参考: zeroclaw-0.1.7/src/tunnel/mod.rs]

### SecuClaw Tailscale隧道
[读取并参考: secuclaw/packages/core/src/tunnel/tailscale/]

### SecuClaw ngrok隧道
[读取并参考: secuclaw/packages/core/src/tunnel/ngrok/]

## 任务要求

1. 在 `secuclaw/packages/core/src/tunnel/custom/` 目录下创建文件：
   - base.ts
   - ssh.ts
   - ws.ts
   - factory.ts
   - registry.ts
   - types.ts
   - 测试文件

2. 设计隧道基类：
   - 抽象类
   - 统一接口
   - 生命周期管理

3. 实现SSH隧道：
   - SSH端口转发
   - 认证支持
   - 错误处理

4. 实现WebSocket隧道：
   - WebSocket代理
   - 双向通信
   - 心跳机制

5. 实现工厂和注册表：
   - 工厂模式
   - 类型注册
   - 动态创建

## 验收标准
- TypeScript无错误
- 支持扩展
- 统一接口

请开始设计，提供完整的代码。
```

---

## 示例代码片段

### base.ts 骨架

```typescript
export abstract class BaseTunnel {
  protected url?: string;
  protected connected: boolean = false;

  /**
   * 连接
   */
  abstract connect(): Promise<void>;

  /**
   * 断开
   */
  abstract disconnect(): Promise<void>;

  /**
   * 检查连接
   */
  isConnected(): boolean {
    return this.connected;
  }

  /**
   * 获取URL
   */
  getUrl(): string {
    if (!this.url) {
      throw new Error('Not connected');
    }
    return this.url;
  }

  /**
   * 注册连接回调
   */
  onConnection(callback: () => void): void {
    // 实现
  }

  /**
   * 注册断开回调
   */
  onDisconnection(callback: () => void): void {
    // 实现
  }
}
```

---

## 隧道接口

```typescript
interface Tunnel {
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  isConnected(): boolean;
  getUrl(): string;
  onConnection(callback: () => void): void;
  onDisconnection(callback: () => void): void;
}
```

---

## 注意事项

1. **可扩展**: 支持自定义隧道类型
2. **统一接口**: 所有隧道使用相同接口
3. **工厂模式**: 使用工厂创建隧道
4. **注册表**: 动态注册隧道类型

---

## 相关链接

- [Factory Pattern](https://en.wikipedia.org/wiki/Factory_method_pattern)
- [SSH Tunneling](https://www.ssh.com/academy/ssh/tunneling/example)
- [WebSocket Protocol](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API)
