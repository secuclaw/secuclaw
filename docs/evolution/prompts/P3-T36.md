# 任务卡片: P3-T36 边缘Web工具

## 任务元数据

```yaml
任务ID: P3-T36
任务名称: Edge Web Tool
Phase: Phase 3 - 边缘部署 + 轻量化
类型: 代码实现
复杂度: ⭐⭐
预估时间: 1.5天
建议LLM: Claude 3.5 Sonnet / GPT-4
依赖: T31
```

---

## 任务描述

实现边缘版简化Web工具，支持基础HTTP请求和安全控制。

---

## 输入资源

### 参考项目

#### ZeroClaw - Web工具
```
zeroclaw-0.1.7/src/tools/
├── web_fetch.rs        # Web获取工具
└── mod.rs              # 工具模块
```

### 目标位置
```
secuclaw/packages/edge/src/tools/web/
├── index.ts           # 主入口
├── fetcher.ts         # HTTP获取
├── cache.ts           # 响应缓存
├── guard.ts           # 安全守卫
└── *.test.ts          # 测试
```

### 现有代码参考
```
secuclaw/packages/edge/src/runtime.ts      # 边缘运行时
secuclaw/packages/core/src/tools/          # 核心工具
```

---

## 输出要求

### 必须产出的文件

1. **index.ts** - 主入口
   ```typescript
   export class EdgeWebTool {
     // 获取URL
     async fetch(url: string): Promise<string>;
     
     // 获取JSON
     async fetchJSON(url: string): Promise<any>;
     
     // 检查URL安全
     isAllowed(url: string): boolean;
   }
   ```

2. **fetcher.ts** - HTTP获取
   - `HTTPFetcher` 类
   - `fetch()` - 获取URL
   - `post()` - POST请求
   - `stream()` - 流式获取

3. **cache.ts** - 响应缓存
   - `ResponseCache` 类
   - `get()` - 获取缓存
   - `set()` - 设置缓存
   - `invalidate()` - 失效缓存

4. **guard.ts** - 安全守卫
   - `WebGuard` 类
   - `validate()` - 验证URL
   - `checkSSRF()` - 检查SSRF
   - `filter()` - 过滤URL

---

## 验收标准

### 功能验收
- [ ] 支持基础HTTP请求
- [ ] 支持响应缓存
- [ ] 内存占用 < 5MB
- [ ] 支持超时控制

### 安全验收
- [ ] SSRF防护
- [ ] URL白名单
- [ ] 大小限制

---

## 技术约束

### 必须遵守
- 验证URL
- 限制大小
- 设置超时
- 缓存响应

### 禁止
- 不要允许SSRF
- 不要无限制请求
- 不要泄露敏感信息

---

## Prompt模板

```
你是一个专业的安全工程师，负责为SecuClaw边缘版实现简化Web工具。

## 背景
SecuClaw边缘版需要基础HTTP请求能力，但资源受限。你需要实现安全的Web工具。

## 参考代码

### ZeroClaw Web获取
[读取并参考: zeroclaw-0.1.7/src/tools/web_fetch.rs]

### ZeroClaw工具模块
[读取并参考: zeroclaw-0.1.7/src/tools/mod.rs]

### SecuClaw边缘运行时
[读取并参考: secuclaw/packages/edge/src/runtime.ts]

### SecuClaw核心工具
[读取并参考: secuclaw/packages/core/src/tools/]

## 任务要求

1. 在 `secuclaw/packages/edge/src/tools/web/` 目录下创建文件：
   - index.ts
   - fetcher.ts
   - cache.ts
   - guard.ts
   - 测试文件

2. 实现HTTP获取：
   - GET请求
   - POST请求
   - 超时控制

3. 实现响应缓存：
   - 缓存响应
   - 失效策略
   - 大小限制

4. 实现安全守卫：
   - URL验证
   - SSRF防护
   - 白名单

## 验收标准
- TypeScript无错误
- 内存 < 5MB
- SSRF防护

请开始实现，提供完整的代码。
```

---

## 示例代码片段

### index.ts 骨架

```typescript
import { HTTPFetcher } from './fetcher';
import { ResponseCache } from './cache';
import { WebGuard } from './guard';

export class EdgeWebTool {
  private fetcher: HTTPFetcher;
  private cache: ResponseCache;
  private guard: WebGuard;

  constructor() {
    this.fetcher = new HTTPFetcher();
    this.cache = new ResponseCache();
    this.guard = new WebGuard();
  }

  /**
   * 获取URL
   */
  async fetch(url: string): Promise<string> {
    // 检查URL安全
    if (!this.isAllowed(url)) {
      throw new Error(`URL not allowed: ${url}`);
    }

    // 检查缓存
    const cached = this.cache.get(url);
    if (cached) {
      return cached;
    }

    // 获取URL
    const response = await this.fetcher.fetch(url);
    
    // 缓存响应
    this.cache.set(url, response);
    
    return response;
  }

  /**
   * 获取JSON
   */
  async fetchJSON(url: string): Promise<any> {
    const response = await this.fetch(url);
    return JSON.parse(response);
  }

  /**
   * 检查URL安全
   */
  isAllowed(url: string): boolean {
    return this.guard.validate(url);
  }
}
```

---

## 注意事项

1. **SSRF防护**: 阻止私有IP和云元数据端点
2. **URL验证**: 验证URL格式和协议
3. **超时控制**: 设置请求超时
4. **缓存策略**: 使用合理的缓存策略

---

## 相关链接

- [Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [SSRF Prevention](https://owasp.org/www-community/attacks/Server_Side_Request_Forgery)
