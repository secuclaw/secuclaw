# 任务卡片: P3-T41 Trait驱动架构

## 任务元数据

```yaml
任务ID: P3-T41
任务名称: Trait-Driven Architecture
Phase: Phase 3 - 边缘部署 + 轻量化
类型: 架构重构
复杂度: ⭐⭐⭐⭐⭐
预估时间: 5天
建议LLM: Claude Opus
```

---

## 任务描述

重构核心模块为Trait驱动架构，支持运行时切换和插件扩展。参考Rust的Trait设计，使用TypeScript接口实现。

---

## 输入资源

### 参考项目

#### ZeroClaw - Trait驱动设计
```
zeroclaw-0.1.7/src/
├── lib.rs              # Trait定义
├── agent/              # Agent Trait
│   └── mod.rs
├── channels/           # Channel Trait
│   └── mod.rs
├── tools/              # Tool Trait
│   └── mod.rs
└── memory/             # Memory Trait
    └── mod.rs
```

### 目标位置
```
secuclaw/packages/core/src/traits/
├── provider.ts        # Provider Trait
├── channel.ts         # Channel Trait
├── tool.ts            # Tool Trait
├── memory.ts          # Memory Trait
├── agent.ts           # Agent Trait
├── registry.ts        # Trait注册表
├── types.ts           # 类型定义
└── architecture.md    # 架构文档
```

### 现有代码参考
```
secuclaw/packages/core/src/providers/  # Provider实现
secuclaw/packages/core/src/channels/   # Channel实现
secuclaw/packages/core/src/tools/      # Tool实现
secuclaw/packages/core/src/agents/     # Agent实现
```

---

## 输出要求

### 必须产出的文件

1. **provider.ts** - Provider Trait
   ```typescript
   export interface ProviderTrait {
     readonly name: string;
     chat(messages: Message[]): Promise<string>;
     chatStream(messages: Message[]): AsyncIterable<string>;
     embed?(text: string): Promise<number[]>;
   }
   ```

2. **channel.ts** - Channel Trait
   ```typescript
   export interface ChannelTrait {
     readonly type: string;
     connect(): Promise<void>;
     disconnect(): Promise<void>;
     send(message: Message): Promise<void>;
     onReceive(handler: MessageHandler): void;
   }
   ```

3. **tool.ts** - Tool Trait
   ```typescript
   export interface ToolTrait {
     readonly name: string;
     readonly description: string;
     execute(params: any): Promise<any>;
   }
   ```

4. **memory.ts** - Memory Trait
   ```typescript
   export interface MemoryTrait {
     store(key: string, value: any): Promise<void>;
     retrieve(key: string): Promise<any>;
     search(query: string): Promise<any[]>;
   }
   ```

5. **agent.ts** - Agent Trait
   ```typescript
   export interface AgentTrait {
     readonly id: string;
     initialize(): Promise<void>;
     execute(task: Task): Promise<Result>;
     terminate(): Promise<void>;
   }
   ```

6. **registry.ts** - Trait注册表
   - `TraitRegistry` 类
   - `register()` - 注册实现
   - `get()` - 获取实现
   - `list()` - 列出实现

---

## 验收标准

### 功能验收
- [ ] 所有核心模块Trait化
- [ ] 支持运行时切换
- [ ] 支持插件扩展
- [ ] 向后兼容

### 架构验收
- [ ] Trait定义清晰
- [ ] 实现可替换
- [ ] 扩展性好

---

## 技术约束

### 必须遵守
- 使用接口定义Trait
- 支持依赖注入
- 支持运行时切换
- 向后兼容

### 禁止
- 不要破坏现有接口
- 不要限制扩展
- 不要耦合实现

---

## Prompt模板

```
你是一个资深架构师，负责将SecuClaw重构为Trait驱动架构。

## 背景
SecuClaw需要更灵活的架构，支持运行时切换和插件扩展。你需要参考ZeroClaw的Trait设计，使用TypeScript接口实现。

## 参考代码

### ZeroClaw Trait定义
[读取并参考: zeroclaw-0.1.7/src/lib.rs]

### ZeroClaw Agent Trait
[读取并参考: zeroclaw-0.1.7/src/agent/]

### ZeroClaw Channel Trait
[读取并参考: zeroclaw-0.1.7/src/channels/]

### ZeroClaw Tool Trait
[读取并参考: zeroclaw-0.1.7/src/tools/]

### ZeroClaw Memory Trait
[读取并参考: zeroclaw-0.1.7/src/memory/]

### SecuClaw Provider实现
[读取并参考: secuclaw/packages/core/src/providers/]

### SecuClaw Channel实现
[读取并参考: secuclaw/packages/core/src/channels/]

### SecuClaw Tool实现
[读取并参考: secuclaw/packages/core/src/tools/]

### SecuClaw Agent实现
[读取并参考: secuclaw/packages/core/src/agents/]

## 任务要求

1. 在 `secuclaw/packages/core/src/traits/` 目录下创建文件：
   - provider.ts
   - channel.ts
   - tool.ts
   - memory.ts
   - agent.ts
   - registry.ts
   - types.ts
   - architecture.md

2. 定义核心Trait接口：
   - ProviderTrait
   - ChannelTrait
   - ToolTrait
   - MemoryTrait
   - AgentTrait

3. 重构现有模块实现Trait：
   - Provider实现
   - Channel实现
   - Tool实现
   - Agent实现

4. 实现Trait注册表：
   - 实现注册
   - 实现查找
   - 运行时切换

5. 确保向后兼容：
   - 保留现有接口
   - 适配器模式
   - 渐进迁移

## 验收标准
- TypeScript无错误
- 所有核心模块Trait化
- 支持运行时切换

请开始设计，提供完整的代码和架构文档。
```

---

## 示例代码片段

### Trait定义示例

```typescript
// Provider Trait
interface ProviderTrait {
  readonly name: string;
  chat(messages: Message[]): Promise<string>;
  chatStream(messages: Message[]): AsyncIterable<string>;
  embed?(text: string): Promise<number[]>;
}

// Channel Trait
interface ChannelTrait {
  readonly type: string;
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  send(message: Message): Promise<void>;
  onReceive(handler: MessageHandler): void;
}

// Tool Trait
interface ToolTrait {
  readonly name: string;
  readonly description: string;
  execute(params: any): Promise<any>;
}

// Memory Trait
interface MemoryTrait {
  store(key: string, value: any): Promise<void>;
  retrieve(key: string): Promise<any>;
  search(query: string): Promise<any[]>;
}
```

---

## 注意事项

1. **接口设计**: 清晰定义Trait接口
2. **依赖注入**: 使用依赖注入管理实现
3. **运行时切换**: 支持运行时切换实现
4. **向后兼容**: 保留现有接口，渐进迁移

---

## 相关链接

- [Rust Traits](https://doc.rust-lang.org/book/ch10-02-traits.html)
- [Dependency Injection](https://en.wikipedia.org/wiki/Dependency_injection)
- [Interface Segregation](https://en.wikipedia.org/wiki/Interface_segregation_principle)
