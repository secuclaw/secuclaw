export const DEFAULT_GATEWAY_PORT = 21000;
export const DEFAULT_GATEWAY_BIND: "loopback" | "all" = "loopback";
export const DEFAULT_TICK_INTERVAL_MS = 1000;
export const DEFAULT_MAX_PAYLOAD_BYTES = 1024 * 1024;
export const DEFAULT_HEARTBEAT_TIMEOUT_MS = 30_000;
