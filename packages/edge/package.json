{
  "name": "@secuclaw/edge",
  "version": "1.0.0",
  "description": "SecuClaw Edge Lite Runtime",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@secuclaw/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.2.0"
  }
}
